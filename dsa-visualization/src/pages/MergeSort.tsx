import {
  useState,
  useEffect,
  useRef,
  forwardRef,
  useImperativeHandle,
} from "react";

type Props = {
  onUpdate: (val: number) => void;
};

type index = {
  value: number;
};

function merge{
  l: number,
  r: Number, 

}: void {

}
